# Keep Docker build context small & avoid Windows path oddities
node_modules
dist
.git
.cache
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Vite/IDE junk
.vscode
.idea

# Git metadata (not needed in build context)
.git
.gitignore
